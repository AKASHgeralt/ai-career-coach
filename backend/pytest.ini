[pytest]
testpaths = tests
markers =
    slow: tests that load the embedding model (~25s). Skip with -m "not slow".
    integration: tests that hit a real database via a throwaway schema.
filterwarnings =
    ignore::DeprecationWarning
    ignore::sqlalchemy.exc.MovedIn20Warning
