[pytest]
testpaths = tests
markers =
    integration: tests that hit a real database via a throwaway schema.
filterwarnings =
    ignore::DeprecationWarning
    ignore::sqlalchemy.exc.MovedIn20Warning
